# 🐛 Cloudflare Workers + Prisma I/O Context Issue

> เอกสารนี้อธิบายปัญหาและวิธีแก้ไขสำหรับ error "Cannot perform I/O on behalf of a different request"

---

## 📋 สรุปปัญหา (Issue Summary)

| รายละเอียด | ค่า |
|------------|-----|
| **วันที่พบ** | 22 มกราคม 2026 |
| **Severity** | High 🔴 |
| **สถานะ** | ✅ แก้ไขแล้ว |
| **ไฟล์ที่แก้ไข** | `src/lib/prisma.ts` |

---

## ❌ Error Messages ที่เจอ

```
Error: Cannot perform I/O on behalf of a different request. 
I/O objects (such as streams, request/response bodies, and others) 
created in the context of one request handler cannot be accessed 
from a different request's handler.
```

```
Error: The Workers runtime canceled this request because it detected 
that your Worker's code had hung and would never generate a response.
```

---

## 🔍 สาเหตุของปัญหา (Root Cause)

### ภาษาไทย

**Cloudflare Workers ทำงานแตกต่างจาก Node.js Server ปกติ**

ใน Node.js/Bun แบบปกติ เราสามารถใช้ **Singleton Pattern** (สร้าง Prisma Client ครั้งเดียวแล้วใช้ร่วมกัน) ได้โดยไม่มีปัญหา

แต่ใน Cloudflare Workers:
1. **หลาย Request ทำงานพร้อมกัน** ใน isolate เดียวกัน
2. **แต่ละ Request มี I/O Context ของตัวเอง** - การอ่านเขียน database, network ฯลฯ ผูกติดกับ request นั้นๆ
3. เมื่อใช้ Singleton Prisma Client ที่สร้างจาก Request A แล้วเอาไปใช้ใน Request B → **Error!**

### แผนภาพแสดงปัญหา

```
┌─────────────────────────────────────────────────────────────────┐
│                    Cloudflare Worker Isolate                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   Request A ──────┐                                              │
│                   │                                              │
│                   ▼                                              │
│           ┌──────────────────┐                                   │
│           │  Singleton       │◄──── สร้าง Prisma Client ครั้งแรก │
│           │  Prisma Client   │      (ผูกกับ I/O Context A)       │
│           │  (Global)        │                                   │
│           └────────┬─────────┘                                   │
│                    │                                              │
│   Request B ───────┴──────────► ใช้ Prisma Client ที่สร้างจาก A  │
│                                                                  │
│                    ❌ ERROR: I/O Context ไม่ตรงกัน!              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## ✅ วิธีแก้ไข (Solution)

### หลักการ: สร้าง Prisma Client ใหม่สำหรับแต่ละ Request

```
┌─────────────────────────────────────────────────────────────────┐
│                    Cloudflare Worker Isolate                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   Request A ────► ┌──────────────────┐                           │
│                   │ Prisma Client A  │ ◄──── I/O Context A ✅    │
│                   └──────────────────┘                           │
│                                                                  │
│   Request B ────► ┌──────────────────┐                           │
│                   │ Prisma Client B  │ ◄──── I/O Context B ✅    │
│                   └──────────────────┘                           │
│                                                                  │
│   Request C ────► ┌──────────────────┐                           │
│                   │ Prisma Client C  │ ◄──── I/O Context C ✅    │
│                   └──────────────────┘                           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### โค้ดที่แก้ไข

**ก่อนแก้ไข (Singleton - ใช้ไม่ได้กับ Workers):**

```typescript
let prismaInstance: PrismaClient | null = null;

export const initPrisma = (env: any) => {
    if (prismaInstance) return; // ❌ ใช้ซ้ำ instance เดิม
    
    const adapter = new PrismaLibSql({ ... });
    prismaInstance = new PrismaClient({ adapter });
};
```

**หลังแก้ไข (Per-Request Pattern):**

```typescript
// Factory function สร้าง client ใหม่ทุกครั้ง
export const createPrismaClient = (env: any): PrismaClient => {
    const adapter = new PrismaLibSql({ ... });
    return new PrismaClient({ adapter }); // ✅ instance ใหม่
};

export const getPrisma = (env?: any): PrismaClient => {
    // Local dev: ใช้ singleton (performance ดีกว่า)
    // Workers: สร้างใหม่ทุก request
    if (isLocalDev) {
        return localPrismaInstance;
    }
    return createPrismaClient(effectiveEnv); // ✅ สร้างใหม่
};
```

---

## 📊 ผลลัพธ์หลังแก้ไข

| ก่อนแก้ไข | หลังแก้ไข |
|-----------|-----------|
| `/vehicles/types` → 500 Error | `/vehicles/types` → 200 OK ✅ |
| `/vehicles/brands` → ทำงานได้บางครั้ง | `/vehicles/brands` → 200 OK ✅ |
| Request timeout บ่อย | ไม่มี timeout |

**Log หลังแก้ไข:**
```
GET /api/v1/private/vehicles/brands 200 520ms ✅
GET /api/v1/private/vehicles/types 200 398ms ✅
GET /api/v1/private/vehicles/brands 200 310ms ✅
GET /api/v1/private/vehicles/types 200 314ms ✅
```

---

## 💡 สิ่งที่ควรจำ (Key Takeaways)

1. **Cloudflare Workers ≠ Node.js Server** - อย่าใช้ pattern เดียวกัน
2. **Singleton Pattern ใช้ไม่ได้** กับ I/O operations ใน Workers
3. **ควรสร้าง Database Client ใหม่** สำหรับแต่ละ request
4. **Local Development ยังใช้ Singleton ได้** - เพื่อ performance ที่ดีกว่า

---

## 🔗 อ้างอิง (References)

- [Cloudflare Workers I/O Context Limitation](https://developers.cloudflare.com/workers/observability/errors/)
- [Prisma with Cloudflare Workers](https://www.prisma.io/docs/guides/deployment/deployment-guides/deploying-to-cloudflare-workers)
